# .env.example - Environment variables for RestInPieces
# Copy this file to .env and fill in the values.
# DO NOT commit the .env file to version control.

# --- JWT Secrets (Generate strong random secrets) ---
# Used to sign and verify authentication tokens.
# Example generation: openssl rand -hex 32
JWT_AUTH_SECRET=

# Used to sign and verify email verification tokens.
JWT_VERIFICATION_EMAIL_SECRET=

# Used to sign and verify password reset tokens.
JWT_PASSWORD_RESET_SECRET=

# Used to sign and verify email change confirmation tokens.
JWT_EMAIL_CHANGE_SECRET=

# --- SMTP Configuration (For sending emails) ---
# Optional: If using SMTP basic auth (plain, login, cram-md5)
SMTP_USERNAME=
# Required if SMTP server needs authentication
SMTP_PASSWORD=
# Optional: Overrides the FromAddress in config.toml if set
# SMTP_FROM_ADDRESS=

# --- OAuth2 Providers (Optional) ---
# Fill these in if you enable the corresponding providers in config.toml

# Google OAuth2 Credentials
# Get these from Google Cloud Console: APIs & Services -> Credentials
OAUTH2_GOOGLE_CLIENT_ID=
OAUTH2_GOOGLE_CLIENT_SECRET=

# GitHub OAuth2 Credentials
# Get these from GitHub: Settings -> Developer settings -> OAuth Apps
OAUTH2_GITHUB_CLIENT_ID=
OAUTH2_GITHUB_CLIENT_SECRET=

# Add any other environment-specific secrets here
